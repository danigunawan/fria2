<h1>Account Management</h1>
<table class = "table">
<% @users.each do |user| %>
  <tr>
      <td><%= link_to user.full_name, view_user_path(user.id) %></td>
      <td><%= link_to "Edit Privileges", edit_account_path(user.id) %></td>
  </tr>
<% end %>
</table>
